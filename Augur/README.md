# Augur — the end-stage synthesis of the IVIM uncertainty program

**Augur** is the *perspective* paper that ties the IVIM uncertainty-quantification program together
into one arc — **trust → value-of-information → action** — across **Fashion** (the ruler),
**Minos** (the decision), **Lethe** (the limits), and **Gauge** (the identifiability wall), anchored
by the **"`D*` cross-modally orphaned"** thread. It makes no new measurement; it argues a synthesis.

> **Status: SUBMISSION-READY but HELD.**
> The manuscript is **complete and compiled** (`paper/augur.pdf`), and `reproduce.sh` is **green
> against the in-repository provisional anchors**. **Submission is held** behind an explicit release
> gate until **Fashion + Minos** publish (Lethe/Gauge recommended). Reproduction and release are
> **separate**: `reproduce.sh` reproduces; `release_gate.py` / `submit.sh` hold. Every project-anchor
> is pinned in [`ASSUMPTIONS.md`](ASSUMPTIONS.md); every external claim cites a **real, checked**
> source in [`CITATIONS.md`](CITATIONS.md); every imported number is tracked in
> [`PROVISIONAL_LEDGER.md`](PROVISIONAL_LEDGER.md).

## The argument in one paragraph

A deployed quantitative-MRI error bar must first be **trusted** (Fashion's calibration ruler: a
skew-aware posterior restores marginal coverage, with a residual high-`D*` hole). Given trust, Minos
prices its **value of information** — the decision–calibration gap `G`, the second-order VoI law
`V = ½|EU″|G² = O(γ²)` (Plumbline Prop. 3, the *Delphi* result), and the label-free detectability
floor. On **action**, two walls appear: the interval is the wrong *size* for real test–retest (Lethe)
and the wrong *parameter* `D*` is unidentifiable (Gauge's CRLB wall). `D*` is the thread where all
three terminate: unidentifiable from its own signal (CRLB grows ~6×, reproduced here), un-scalable to
its own repeatability (`r=−0.17`, 95% bootstrap CI `[−0.39, +0.05]`, spanning zero), and only
weakly/inconsistently tied to DCE `Ktrans` (`r=0.389` Sun 2019; null Yang 2019). The defensible
end-stage claim: **the value of a calibrated error bar is real exactly where the parameter is
identifiable — and `D*` marks the wall.** See [`synthesis.md`](synthesis.md) and `paper/augur.tex`.

## Layout

```
Augur/
  paper/
    augur.tex            the manuscript (house style; \input{numbers.tex})
    numbers.tex          AUTO-GENERATED by consistency.py (every number traces to an anchor)
    refs.bib             bibliography (verified lit + forward-cited in-program anchors; no fake DOIs)
    consistency.py       CP4 gate: regenerate numbers.tex; verify macros + spine invariants
    build.sh             regenerate anchors -> consistency -> tectonic augur.pdf
    augur.pdf            compiled manuscript (6 pp)
  anchors/
    extract_anchors.py   parse committed Gauge results + CITATIONS -> anchors.json (traceability)
    anchors.json         AUTO-GENERATED load-bearing anchor values + provenance
  scripts/
    crlb_wall.py         re-derive the CRLB identifiability wall (numpy-only) + figure + cross-check
    retest_ci.py         D* test-retest interval: carry BCa bootstrap CI + reproduce Fisher-z CI
    dstar_ktrans.py      external D*-Ktrans evidence table (Sun/Yang), verified
  figures/crlb_wall.pdf  AUTO-GENERATED identifiability-wall figure
  results/*.json         AUTO-GENERATED reproduction outputs
  reproduce.sh           GREEN reproduction pipeline (anchors -> numbers -> tests); exit 0
  release_gate.py        the HOLD: FASHION_PUBLISHED + MINOS_PUBLISHED gate (exit 1 = HELD)
  release_config.json    the only place the hold is keyed (flags + DOIs; both false today)
  submit.sh              submit path; HALTS with "HELD — awaiting Fashion + Minos publication"
  SUBMISSION_HOLD        AUTO-GENERATED marker the final report reads (ACTIVE today)
  PROVISIONAL_LEDGER.md  every imported number + the re-check that fires on acceptance
  synthesis.md  ASSUMPTIONS.md  CITATIONS.md  SUBMISSION_BLOCK.md
```

## Reproduce (green) vs. release (held) — separate concerns

```bash
bash reproduce.sh          # GREEN: regenerates every in-repo anchor + numbers.tex + tests (exit 0)
bash paper/build.sh        # regenerate numbers.tex and compile paper/augur.pdf (tectonic)
python3 release_gate.py    # the HOLD gate: exit 1 = HELD (expected today), 0 = clear to submit
bash submit.sh             # refuses while held; prints which conditions are unmet
```

`reproduce.sh` is green **whether or not** the paper may be submitted. The submission HOLD lives only
in the release gate. To lift it, set `published=true` **with a real DOI** for Fashion and Minos in
`release_config.json` per [`PROVISIONAL_LEDGER.md`](PROVISIONAL_LEDGER.md) — never without a DOI.

## Provenance & IP

Created in-repo (clean, argument-only history; no data in tree or history), mirroring the
`Caliper/` · `Sextant/` · `Gnomon/` pattern. **Clean IP:** Augur touches no `pancData3` / MSK /
private clinical data — it reads in-repo synthetic / open-data (ACRIN-6698, CC-BY-4.0) results and
published literature only. Each anchor subrepo's own `README.md` is authoritative for its status.
