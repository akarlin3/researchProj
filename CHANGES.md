# CHANGES — needle/format (NMR in Biomedicine format pack)

Needle-mover 2/3. Adds the **NMR-in-Biomedicine required format items** to the
submission-ready railing-first paper. **No new scientific claims.** Every number
traces to Gnomon/Sextant through `numbers.tex`; the consistency gate confirms
**no result number changed** (byte-identical `numbers.tex`).

**Target manuscript:** `Fashion/paper_retool/manuscript.tex` — the HEAD
boundary-railing-first submission package (the one with the one-command consistency
gate `consistency.py`; the older `Fashion/paper/` variant already carried these
items and is not the submission draft). The format items below were **ported** from
that sibling and re-expressed via `paper_retool`'s macros so they stay gate-guarded.

---

## 1. Located (audit-first)

- **Abstract:** `Fashion/paper_retool/manuscript.tex`, `\begin{abstract}` block.
  *Was* a structured Purpose/Methods/Results/Conclusion abstract (≈297 words).
- **Figures:** the manuscript body was tables-only (Tables 1–3); GA asset produced
  here at `Fashion/paper_retool/figures/graphical_abstract.{pdf,png}`.
- **Declarations present before this change:** only *Data availability and
  reproducibility*. (No ethics / contributions / COI / funding / AI declaration.)

## 2. Graphical abstract

- **Figure (50×60 mm, legible):** `Fashion/paper_retool/figures/graphical_abstract.pdf`
  (and `.png`, 600 dpi). PDF MediaBox verified **exactly 50.0 × 60.0 mm**.
  Regenerated by `Fashion/paper_retool/make_graphical_abstract.py` directly from
  `Sextant/results/railing_results.json`; the four bars are **exactly the four rows
  of Table 1** (Abdomen homogeneous 54.7%, Abdomen full 47.8%, Liver 4-*b* 43.7%,
  Liver 3-*b* 73.4%), so the GA can never drift from the table or `numbers.tex`.
- **GA text — 78 words, 2 sentences (limit ≤80 words, ≤3 sentences):**

  > A conventional bounded least-squares IVIM fit rails the pseudo-diffusion
  > coefficient *D\** against a parameter bound in **54.7%** of high-SNR voxels of an
  > open in-vivo abdomen scan (95% CI [52.2, 57.1]) — an assumption-free,
  > reproducible signature of weak *D\** identifiability that recurs at
  > **43.7–73.4%** across independent abdomen and liver cohorts. Because the fraction
  > is read directly off the optimiser, it needs no ground truth and is immune to the
  > objection that a calibration claim inherits the trust of its simulator.

  (In the source the numbers are the macros `\HomoFrac`, `\HomoCIlo/\HomoCIhi`,
  `\LihcFourFrac`, `\LihcThreeFrac` — i.e. gate-guarded, not hand-typed.)

## 3. 300-word abstract summary (single prose block)

The structured abstract was converted into one prose paragraph, **301 words**
(target ≈300), preserving every number exactly via the existing macros and adding
no claim absent from the manuscript body. Location:
`Fashion/paper_retool/manuscript.tex`, `\begin{abstract}` … `\end{abstract}`.
Numbers retained: 54.7% [52.2,57.1] (n=1,618), clean-room 54.2% [52.0,56.4], prior
54.7%, full-ROI 47.8%, liver 43.7% / 73.4% (3 subjects), high-*D\** under-coverage
0.63 Laplace / 0.81 MCMC, marginal quantile 0.90, residual 0.81, flow 0.98 vs 0.76,
sharpness 0.112 vs 0.181.

## 4. AI-use declaration (in the manuscript declarations)

Added **`\section*{Declaration of generative AI and AI-assisted technologies}`** to
`Fashion/paper_retool/manuscript.tex` (declarations group, immediately before the
bibliography). Wording is **identical** to the sibling `Fashion/paper/manuscript.tex`
("…large language model (Claude Opus 4.8, Anthropic)… no AI tool was used to create,
alter, or interpret research data or results…"). It is now in the **manuscript**,
not only the cover letter. The same insertion also supplies the mandatory companion
declarations (Data ethics, Author contributions, Conflict of interest, Funding) that
the package was missing — pure administrative text, no numbers, no scientific claims.

## 5. Consistency gate (one command)

```
cd Fashion/paper_retool && python3 consistency.py     # numbers-gate OK: 126 macros
```

`numbers.tex` md5 **`d4e43c029cc525f5a5597dd478bd1a75`** before and after — i.e.
**byte-identical → no result number changed**. Manuscript rebuilds clean with
`tectonic` (`bash build.sh` also runs the gate then compiles).

---

## Confirmations (final gate)

| Item | Status |
|---|---|
| GA text ≤ 80 words | **78 words**, 2 sentences ✓ |
| Summary ≈ 300 words | **301 words** ✓ |
| GA figure 50×60 mm | MediaBox 50.0×60.0 mm ✓ |
| AI declaration in manuscript declarations | present, wording matched ✓ |
| No result number changed | `numbers.tex` byte-identical (md5 unchanged) ✓ |
| New scientific claims | **none** (format-only; numbers via existing macros) ✓ |

## NOT submission-ready — needs author sign-off

Per the prompt's final gate: **do not mark submission-ready until the author
approves the GA figure choice and text.** Two author decisions remain:

1. **Approve the GA figure + 78-word text** above.
2. **Confirm the target.** This work assumes `Fashion/paper_retool/` is the chosen
   submission draft (HEAD package + consistency gate). The older `Fashion/paper/`
   variant already carries equivalent format items; if *that* is the draft, no
   action is needed there. Remaining Wiley-template / reference-style formatting is
   still pending (see `Fashion/paper_retool/README.md`).


---

# CHANGES — needle/replication (cross-cohort replication reframe)

Branch `needle/replication`. **Framing only — no number changed, none dropped.** The
least-clean cohort (73.4%, liver 3-b) is no longer the headline cross-organ number; the
clean 4-b liver (43.7%, with a proper `b=0`) is now the primary independent cross-organ
test, and the 73.4% is retained as directionally-consistent supporting evidence with its
limitations stated at first mention.

Gnomon/Sextant remain the source of truth. No value was edited, recomputed, or invented.

## Edits

1. **Abstract** (`Fashion/paper/manuscript.tex`, ~line 91). The liver sentence now names
   the 4-b cohort (43.7%) as *our primary independent cross-organ test* (unrelated organ,
   scanner, site, proper `b=0`). The 3-b 73.4% is presented after it as a sparser scheme
   that "rails still more … directionally consistent with the same mechanism."

2. **Results §3.1 replication paragraph** (`Fashion/paper/manuscript.tex`, ~lines 286–292).
   - 4-b promoted to the load-bearing cross-organ test: "Our primary independent
     cross-organ test is a multi-subject liver DWI cohort (TCGA-LIHC … proper `b=0` …):
     under a clean four-b-value scheme D\* rails in **43.7% (43.2–44.2)** … reproducing
     the phenomenon in an entirely unrelated acquisition."
   - 3-b reframed as supporting, **limitations stated at first mention**: "A sparser
     three-b-value liver scheme rails still more — **73.4% (73.0–73.9)** — which we report
     as supporting rather than load-bearing evidence: it omits `b=0`, is normalized by its
     lowest acquired `b` (an approximation), and pools only three subjects (per-subject
     rates 0.66, 0.74, 0.75 …)." Its higher rate is tied to the mechanism (thinner low-`b`
     perfusion sampling → weaker D\* identifiability), exactly as predicted.

3. **Figure 1 caption** (`Fashion/paper/manuscript.tex`, ~line 455). The clean four-b-value
   liver scheme (proper `b=0`) is named the primary independent cross-organ test; the
   three-b scheme "rails most, supporting the same mechanism."

4. **Table 1 caption** (`Fashion/paper/supplement.tex`, ~line 68). Added that the 4-b liver
   cohort is the primary independent cross-organ test (REPLICATES-STRONG), while the 3-b
   cohort is "supporting evidence only — it omits `b=0`, is normalized by its lowest
   acquired `b`, and pools just three subjects" (REPLICATES, no wide-bound control). Row
   order (4-b before 3-b) and verdict emphasis (REPLICATES-STRONG vs REPLICATES) were
   already correct and are unchanged.

## Numbers gate

The applicable proof for a framing-only edit is a **freeze-before/after diff** of the PDF
numeric multiset (`freeze_numbers.sh`), built with `tectonic`:

Human-readable summary of `diff numbers_pre.txt numbers_post.txt`:

- `manuscript.pdf`: 10 `"0"` → 14 `"0"` (+4 from added `$b=0$` wording)
- `supplement.pdf`: 6 `"0"` → 8 `"0"` (+2 from added `$b=0$` wording)

The **only** change is an *increase* in the count of the token `0`, from the six added
`$b=0$` mentions. Every reported result token is unchanged:

- All four cohort rates intact: **54.2/54.7%** (abdomen homogeneous), **47.8%** (abdomen
  full), **43.7%** (liver 4-b), **73.4%** (liver 3-b).
- All CI bounds intact (43.2–44.2, 73.0–73.9, 47.1–48.5, 52.0–56.4, 52.2–57.1, …).
- Per-subject 3-b rates (0.66, 0.74, 0.75) intact.

A line-level diff of just the cohort-number tokens is empty (byte-identical pre vs post).

## Confirmation

- [x] All four cohort numbers unchanged (verified by multiset diff).
- [x] 4-b (43.7%, proper `b=0`) framed as the primary independent cross-organ test.
- [x] 73.4% retained, with its limitations (no `b=0`, lowest-`b` normalization, three
      subjects) stated at first mention in both the abstract and Results §3.1.
- [x] Table 1 ordering/emphasis and abstract reflect 4-b primary / 3-b supporting.

**NOT submission-ready:** do not mark submission-ready until the author approves the
reframed replication paragraph.
