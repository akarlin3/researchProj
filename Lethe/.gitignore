# Real in-vivo DWI: download-on-demand pixel/array data (CC-BY-4.0 ACRIN-6698, NOT
# redistributed here -- fetch with scripts/fetch_invivo.py; the provenance manifest
# results/invivo_provenance.json IS committed, the data is not).
data/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.eggs/

# Reproducible caches (regenerated by seeds)
results/cohorts/
*.npz

# LaTeX intermediates (the built paper PDF is tracked deliberately)
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.toc

# OS
.DS_Store
