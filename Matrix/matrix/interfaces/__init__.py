"""The three consumed-component interfaces, each with a labelled placeholder.

  ruler.py  — Fashion (calibrated error bars)
  gates.py  — Minos (trust gate + action gate)
  dose.py   — Forge (dose engine; deferred to 2027, placeholder only)
"""
