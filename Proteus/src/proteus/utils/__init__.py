"""Utils — shared helpers (config loading, seeding, IO).

Keep cross-stage helpers here so every stage reads config/proteus.yaml the same
way and seeds RNGs from the single global random_seed.
"""

# TODO(P0): add load_config(), get_seed(), and PDB/FASTA IO helpers used across stages.
